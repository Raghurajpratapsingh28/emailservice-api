/**
 * Vitest setup. Env vars are injected via vitest.config.ts `test.env`, which is
 * applied before any module loads. This setup file is reserved for runtime hooks
 * (timers, mocks) — currently empty by design.
 */
export {};
